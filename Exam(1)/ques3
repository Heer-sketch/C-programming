#include <stdio.h>

int main() 
{
    int n, i = 1, sum = 0;
    printf("Enter the value of n: ");
    scanf("%d", &n);
    do
     {
        sum += i;  
        i += 2;   
    } while (i <= n);
    printf("The sum of all odd numbers from 1 to %d is  %d\n", n, sum);
    return 0;
}
